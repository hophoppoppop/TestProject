import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  pagination: {
    height: 100,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  dot: {
    marginHorizontal: 5,
  },
});
