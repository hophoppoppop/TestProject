import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  pagination: {
    height: 100,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  scrollViewContainer: {
    flex: 1,
  },
  dot: {
    marginHorizontal: 5,
  },
});
